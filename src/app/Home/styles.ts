import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#333',
      alignItems: 'center',
      justifyContent: 'center',
    },
    text: {
      color: "#999",
      fontSize: 25,
      textAlign: "center"
    },
    input: {
      backgroundColor: "#999",
      width: 200,
      borderRadius: 20
    },
    Button: {
      width: "100%",
      backgroundColor: "#fff"
    },
    Imagens: {
      width: 200,
      height: 200,
      borderRadius: 20
    }
  });


// export const getStyles = (theme: string | null | undefined) => StyleSheet.create({

// })